server.port: 8888
management:
  endpoints:
    web:
      exposure:
        include: "*"
spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          addLabelLocations: false
